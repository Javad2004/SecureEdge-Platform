# Platform Examples

This directory is reserved for examples that exercise the complete SecureEdge Platform rather than one application in isolation.

Application-specific configuration examples currently live under:

- `apps/edgeproxy/configs/examples/`
- `apps/securityedge/configs/`

Add future end-to-end request samples, route scenarios, and security-policy demonstrations here only when they depend on both EdgeProxy and SecurityEdge.
