diff -ruN '--exclude=.git' a/cmd/edgeproxy/main.go b/cmd/edgeproxy/main.go
--- a/cmd/edgeproxy/main.go	2026-08-04 23:35:14.444716644 +0000
+++ b/cmd/edgeproxy/main.go	2026-08-04 23:35:25.389243616 +0000
@@ -11,6 +11,7 @@
 	"github.com/Javad2004/SecureEdge-Platform/apps/edgeproxy/internal/config"
 	"github.com/Javad2004/SecureEdge-Platform/apps/edgeproxy/internal/envfile"
 	"github.com/Javad2004/SecureEdge-Platform/apps/edgeproxy/internal/server"
+	securityedge "github.com/Javad2004/SecureEdge-Platform/apps/securityedge"
 )
 
 func main() {
@@ -26,6 +27,7 @@
 		os.Exit(1)
 	}
 	loadedEnv := ""
+	loadedSecurityEnv := ""
 	_, configEnvironmentPreexisting := os.LookupEnv("EDGEPROXY_CONFIG")
 	var err error
 	if !*noEnv {
@@ -45,6 +47,29 @@
 		"apps/edgeproxy/configs/edgeproxy.json",
 	)
 
+	if !*noEnv {
+		_, securityConfigEnvironmentPreexisting := os.LookupEnv("SECURITYEDGE_CONFIG")
+		explicitSecurityEnv := strings.TrimSpace(os.Getenv("SECURITYEDGE_ENV_FILE"))
+		loadedSecurityEnv, err = envfile.Load(explicitSecurityEnv, "apps/securityedge/.env", "../securityedge/.env")
+		if err != nil {
+			fmt.Fprintln(os.Stderr, err)
+			os.Exit(1)
+		}
+		securityConfig := resolveConfigPath(
+			"",
+			os.Getenv("SECURITYEDGE_CONFIG"),
+			loadedSecurityEnv,
+			!securityConfigEnvironmentPreexisting,
+		)
+		if securityConfig != "" {
+			if err := os.Setenv("SECURITYEDGE_CONFIG", securityConfig); err != nil {
+				fmt.Fprintln(os.Stderr, "set SECURITYEDGE_CONFIG:", err)
+				os.Exit(1)
+			}
+		}
+	}
+	securityConfigPath := strings.TrimSpace(os.Getenv("SECURITYEDGE_CONFIG"))
+
 	var handler slog.Handler
 	if *prettyLogs {
 		handler = slog.NewTextHandler(os.Stdout, &slog.HandlerOptions{Level: slog.LevelInfo})
@@ -55,6 +80,9 @@
 	if loadedEnv != "" {
 		logger.Info("environment file loaded", "path", loadedEnv)
 	}
+	if loadedSecurityEnv != "" {
+		logger.Info("security environment file loaded", "path", loadedSecurityEnv)
+	}
 
 	cfg, err := config.Load(configPath)
 	if err != nil {
@@ -62,6 +90,12 @@
 		os.Exit(1)
 	}
 	if *validateOnly {
+		if securityConfigPath != "" {
+			if err := securityedge.Validate(securityConfigPath); err != nil {
+				logger.Error("security configuration error", "error", err)
+				os.Exit(1)
+			}
+		}
 		fmt.Println("configuration is valid")
 		return
 	}
diff -ruN '--exclude=.git' a/go.mod b/go.mod
--- a/go.mod	2026-08-04 23:23:48.000000000 +0000
+++ b/go.mod	2026-08-04 23:35:25.389352648 +0000
@@ -1,3 +1,7 @@
 module github.com/Javad2004/SecureEdge-Platform/apps/edgeproxy
 
 go 1.26
+
+require github.com/Javad2004/SecureEdge-Platform/apps/securityedge v0.0.0
+
+replace github.com/Javad2004/SecureEdge-Platform/apps/securityedge => ../securityedge
diff -ruN '--exclude=.git' a/internal/server/run.go b/internal/server/run.go
--- a/internal/server/run.go	2026-08-04 23:23:48.000000000 +0000
+++ b/internal/server/run.go	2026-08-04 23:35:25.389419377 +0000
@@ -8,6 +8,7 @@
 	"net/http"
 	"os"
 	"os/signal"
+	"strings"
 	"sync"
 	"syscall"
 
@@ -16,6 +17,7 @@
 	"github.com/Javad2004/SecureEdge-Platform/apps/edgeproxy/internal/config"
 	"github.com/Javad2004/SecureEdge-Platform/apps/edgeproxy/internal/metrics"
 	"github.com/Javad2004/SecureEdge-Platform/apps/edgeproxy/internal/proxy"
+	securityedge "github.com/Javad2004/SecureEdge-Platform/apps/securityedge"
 )
 
 func Run(cfg config.Config, logger *slog.Logger) error {
@@ -30,8 +32,26 @@
 	}
 	defer handler.Close()
 
+	mainHandler := http.Handler(handler)
+	var securityRuntime *securityedge.Runtime
+	var securityAdminServer *http.Server
+	if securityConfig := strings.TrimSpace(os.Getenv("SECURITYEDGE_CONFIG")); securityConfig != "" {
+		securityRuntime, err = securityedge.New(securityConfig, logger)
+		if err != nil {
+			return fmt.Errorf("securityedge initialization: %w", err)
+		}
+		defer securityRuntime.Close()
+		mainHandler = securityRuntime.Wrap(handler)
+		if securityRuntime.Config().Admin.Enabled {
+			securityAdminServer, err = securityRuntime.AdminServer()
+			if err != nil {
+				return fmt.Errorf("securityedge admin initialization: %w", err)
+			}
+		}
+	}
+
 	mainServer := &http.Server{
-		Addr: cfg.Server.ListenAddr, Handler: handler,
+		Addr: cfg.Server.ListenAddr, Handler: mainHandler,
 		ReadHeaderTimeout: cfg.Server.ReadHeaderTimeout.Duration,
 		ReadTimeout:       cfg.Server.ReadTimeout.Duration,
 		WriteTimeout:      cfg.Server.WriteTimeout.Duration,
@@ -39,7 +59,7 @@
 		MaxHeaderBytes:    cfg.Server.MaxHeaderBytes,
 	}
 
-	errCh := make(chan error, 2)
+	errCh := make(chan error, 3)
 	go func() {
 		logger.Info("proxy server starting", "address", cfg.Server.ListenAddr, "tls", cfg.Server.TLS.Enabled)
 		var serveErr error
@@ -53,6 +73,15 @@
 		}
 	}()
 
+	if securityAdminServer != nil {
+		go func() {
+			logger.Info("securityedge admin server starting", "address", securityAdminServer.Addr)
+			if serveErr := securityAdminServer.ListenAndServe(); serveErr != nil && !errors.Is(serveErr, http.ErrServerClosed) {
+				errCh <- fmt.Errorf("securityedge admin server: %w", serveErr)
+			}
+		}()
+	}
+
 	var adminServer *http.Server
 	if cfg.Admin.Enabled {
 		adminServer = admin.New(cfg.Admin, logger, registry, handler, logStore).HTTPServer()
@@ -78,7 +107,8 @@
 	defer cancel()
 	shutdownErr := shutdownServers(shutdownCtx,
 		namedHTTPServer{name: "proxy", server: mainServer},
-		namedHTTPServer{name: "admin", server: adminServer},
+		namedHTTPServer{name: "edgeproxy admin", server: adminServer},
+		namedHTTPServer{name: "securityedge admin", server: securityAdminServer},
 	)
 	return errors.Join(runErr, shutdownErr)
 }
